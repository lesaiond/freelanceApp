import styled from "styled-components";
import { newColors } from "../../../style/__colors";

const {elemsBgc} = newColors

export const StyledGrayBox = styled.div`
  border-radius: 20px;
  background: ${elemsBgc};
`